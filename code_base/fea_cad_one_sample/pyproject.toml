[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fea-cad-one-sample"
version = "0.0.0"
description = "One-sample CAD-to-FEA prototype"
requires-python = ">=3.10"
